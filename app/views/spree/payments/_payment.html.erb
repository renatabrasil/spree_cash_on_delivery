<% source = payment.source %>
<% if source.is_a?(Spree::CreditCard) %>
  <span class="cc-type">
    <% unless (cc_type = source.cc_type).blank? %>
      <%= image_tag "credit_cards/icons/#{cc_type}.png" %>
    <% end %>
    <% if source.last_digits %>
      <%= Spree.t(:ending_in) %> <%= source.last_digits %>
    <% end %>
  </span>
  <br />
  <span class="full-name"><%= source.name %></span>
<% else %>
  <%= content_tag(:span, payment.payment_method.name) %><br>
  <% if payment.payment_method.is_a?(Spree::PaymentMethod::Check) && !payment.change.blank? %>
    Troco: R$<%= content_tag(:span, payment.change) %>
  <% end %>
<% end %>
